#version 430

layout(std430, binding=0) buffer Pos {
    vec4 Position[];
};

layout(std430, binding=1) buffer Vel {
    vec4 Velocity[];
};

void main() {
    uint idx = gl_GlobalInvocationID.x;

    Position[idx] = vec4(0); // TODO
    Velocity[idx] = vec4(0); // TODO
}
